<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Ukuran Bulatan -->
    <size
        android:width="20dp"
        android:height="20dp" />

    <!-- Warna Isi (Putih) -->
    <solid android:color="#FFFFFF" />

    <!-- Garis Tepi (Hitam Tipis) -->
    <stroke
        android:width="2dp"
        android:color="#000000" />
</shape>